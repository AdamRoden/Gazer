# mapping

JSON **profiles** map layout `command` names (and optional speak typing) to
`InputService` outputs.

- Profile: `resources/mappings/default.json`
- Loader: `MappingLoader`
- Runtime: `MappingEngine::runCommand` / `handleSpeak`
